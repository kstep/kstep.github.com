{% include "_layouts/header.liquid" %}
<a class="btn-floating btn-large waves-effect waves-light purple lighten-3 right" href="https://github.com/kstep/kstep.github.com/new/master/_src/posts/" style="top: -30px; position: relative;"><i class="material-icons">add</i></a>

<h2>All posts</h2>
<div class="row">
    {% for post in posts %}
        <div class="col s12 m6 l4"><a href="{{ post.path }}">
            <div class="card">
                <div class="card-content">
                    <span class="card-title">{{ post.title }}</span>
                    {% if post.lang %}<label class="emoji">{% if post.lang == "en" %}🇺🇸{% endif %}{% if post.lang == "ru" %}🇷🇺{% endif %}</label>{% endif %}
                    <label>{{ post.date | date:"%a&nbsp;%d&nbsp;%b&nbsp;'%y" }}</label>
                    {% if post.excerpt %}<p class="black-text">{{ post.excerpt }}</p>{% else %}<br />{% endif %}
                    {% if post.tags %}<label>{{ post.tags }}</label>{% endif %}
                </div>
            </div>
            </a></div>
    {% endfor %}
</div>
<script src="/static/masonry.pkgd.min.js"></script>
<script>var msnry = new Masonry(document.querySelector(".row"), { querySelector: ".col" });</script>
{% include "_layouts/footer.liquid" %}
